# Configuration environment vars for Jarvis Contact

# Replace the IP and port with your hosted JARVIS endpoint
JARVIS_API_URL="0.0.0.0:50051"
# NER model to use in Jarvis
JARVIS_NER_MODEL="jarvis_ner_i2b2"
# NER entities to use from the above model (can be a subset of what is offered)
JARVIS_NER_ENTITIES="problem,test,treatment"

# The port your Node.js app will be hosted at
PORT="8009"
# Port for the peer-js server, to be used for negotiating the peer-to-peer chat connection
PEERJS_PORT="9000"
